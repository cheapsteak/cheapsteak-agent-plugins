# macos-ops

macOS sysadmin.

## Skills

- **diagnose-memory** — Diagnose macOS memory usage and identify what's consuming RAM. Use when the system is slow, swap is high, or Activity Monitor shows suspicious numbers.

## Requirements

- macOS (skills use `vm_stat`, `top`, `ps`, etc.)
